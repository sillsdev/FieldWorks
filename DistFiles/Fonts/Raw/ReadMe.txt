Files in this DistFiles\Fonts\Raw folder will not automatically be installed.
This folder is intended for raw .ttf files that need to be instaled to the end-user's
Windows\Fonts folder. The files in this folder are not picked up automatically.
Each one is manually configured into the installer, via the Installer\Files.wxs file.