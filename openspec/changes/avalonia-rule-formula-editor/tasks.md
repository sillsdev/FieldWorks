# Tasks

> The Grammar rule-editing family extracted from `lexical-edit-avalonia-migration` §20.3.4 (GB-*) as its
> own XL project. Builds on the Phase-1 class-general composer + `RegionEditorPluginRegistry` +
> `RegionEditContextBase` fenced-UOW seam. Architecture rule (design Decision 1): rendering in FwAvalonia,
> ALL LCModel reads/writes in the xWorks/Morphology plugin. Each block build+test-gated; gated behind
> UIMode=New. Read-only first, then editable.

## 0. Testing & reuse standards (apply to every block)
- **Test rubric per block** (mandatory): **T1** unit; **T2** integration (the editor composed/realized on one surface, no-Unsupported); **T4 headless workflow** over a real in-memory cache (edit → commit as ONE undo step → reopen → round-trips; incl. the editable write-back); **T5** PNG + crowding sanity. A block is not done until its editable behavior has a T4.
- **Harnesses (reuse, do not reinvent):** cache-backed T1/T2/T4 inherit `MemoryOnlyBackendProviderTestBase` (SIL.LCModel.Infrastructure) — canonical pattern `StructuredTextWorkflowTests` / `StructuredTextIntegrationTests` (headless `FwAvaloniaRuntime.EnsureInitialized()`); T5 uses `DialogSnapshot.Capture(surface, "RuleFormulaEditor-<NN>-<stage>")` (Src/Common/FwAvalonia/FwAvaloniaTests/Visual/DialogSnapshot.cs) + `DialogLayoutAssert.AssertNoCrowding(root)` (Src/Common/FwAvaloniaDialogs/FwAvaloniaDialogsTests/DialogLayoutAssert.cs). `EngineIsolationAuditTests` (FwAvaloniaTests) MUST stay green (no native Views/Graphite/LCModel in the FwAvalonia editor).
- **Reuse (architecture):** plug in via `RegionEditorPluginRegistry` (mirror `DialogLauncherPlugins`), compose via the §20.1.4 class-general `FullEntryRegionComposer.Compose(ICmObject,…)`, and stage ALL writes through the region's existing fenced `LcmRegionEditSession` via `IRegionEditContext` — the plugin does NOT open its own session. Cell choosers reuse `FwOptionPicker` + `RegionValueFactory.BuildPossibilityOptions()` (`RegionChoiceOption.Depth` for the natural-class hierarchy); no new picker control.

## 1. Cell-model projection + read-only grid (de-risk)
- [x] 1.1 DONE+green: LCModel-free `RuleCell { Kind, DisplayText, TargetGuid }` + `RuleFormulaModel` (ordered cells, rule kind; immutable Insert/Remove/MoveCell helpers) — `Src/Common/FwAvalonia/Region/RuleFormulaModel.cs`; 5 unit tests (`RuleFormulaModelTests`).
- [x] 1.2 DONE+green: projector `RuleFormulaProjector.ProjectRegularRule(IPhSegRuleRHS)` → sectioned `RuleFormulaModel` (LHS/RHS/left+right context) — `Src/xWorks/RuleFormulaProjector.cs`; T1 parity vs hand-written legacy-Vc oracle string (`RuleFormulaProjectorTests`, 2 tests, MemoryOnlyBackendProviderTestBase). `// PARITY`: feature-multiline NC + iteration min/max deferred to block 2.
- [x] 1.3 DONE+green: read-only `RuleFormulaRegionEditor` (`Src/Common/FwAvalonia/Region/RuleFormulaRegionEditor.cs`) renders the sectioned cell grid (bare tokens, NC bracketed as chrome, role separator glyphs, empty-group placeholder boxes); T5 PNGs `RuleFormulaEditor-01-regular` / `-02-empty-groups` + AssertNoCrowding (`RuleFormulaRegionEditorTests`, 2 tests). Model reworked to sectioned shape (`RuleFormulaModel`/`RuleFormulaSection`); `RuleFormulaModelTests` updated (8 tests).
- [x] 1.4 DONE+green: `RuleFormulaRegionEditorPlugin` (`Src/xWorks/RuleFormulaRegionEditorPlugin.cs`) claims `RegRuleFormulaSlice`, projects the RHS, returns the read-only editor; registered in `RegionEditorPlugins.RegisterBuiltins`. T2 `RuleFormulaRegionEditorPluginTests` (3 tests): direct build, registered-in-Default, and real `FullEntryRegionComposer.Compose(rhs,"Edit")` resolves the editor — no §20.1.3 Unsupported row.

## 2. Editable rule-formula grid
- [x] 2.1 DONE+green: LCModel-free `IRuleCellCommandSink` + `RuleCellSpec` (`Src/Common/FwAvalonia/Region/RuleCellCommands.cs`); `RuleFormulaEditSink` (`Src/xWorks/RuleFormulaEditSink.cs`) applies insert/delete/reorder/set across ALL four regular-rule sections — LHS/RHS owning sequences AND left/right CONTEXT slots with the atomic↔`PhSequenceContext` 0→1→2→1→0 transition (legacy `CreateSeqCtxt`/`InsertContextInto`/`RemoveContextsFrom` parity) — via the host's shared fenced session (`RegionEditContextBase.Stage` + commit-immediately = one undo step) then re-projects; `RuleFormulaRegionEditor.Sink` wired by the plugin. T4 `RuleFormulaEditWorkflowTests` (6 tests): delete/insert/move/context-atomic↔sequence round-trips + one-undo-step + undo-restore + reject-unresolvable; EngineIsolationAudit green. // PARITY: context members assumed simple contexts (iteration/nested-seq deferred, as in the projector).
- [x] 2.2 DONE+green: `RuleFormulaOptions.BuildCellOptions` (`Src/xWorks`) projects phonemes/natural-classes/boundaries → `RegionChoiceOption`s with `RuleCellSpec` codec keys (`ToOptionKey`/`FromOptionKey` in `RuleCellCommands.cs`); the editable `RuleFormulaRegionEditor` renders each cell as an `FwOptionPicker`-backed chooser button (click=replace, context-menu Delete + Insert-before) plus a trailing "+" append button per section; commit decodes the key → sink. Tests: codec round-trip (`RuleCellCommandsTests`), options T1 (`RuleFormulaOptionsTests`), editable-grid PNG `RuleFormulaEditor-03-editable` + AssertNoCrowding. // PARITY: drag-reorder UI deferred (sink `MoveCell` is T4-tested).
- [x] 2.3 DONE+green: metathesis roles (LeftEnv/LeftSwitch/RightSwitch/RightEnv); `RuleFormulaProjector.ProjectMetathesisRule` buckets the index-partitioned `StrucDescOS` via `GetStrucChangeIndex`; `MetaRuleFormulaRegionEditorPlugin` (registered) + editable `MetaRuleFormulaEditSink` (insert/delete/set maintain partitions via `UpdateStrucChange`/`DeleteObjectSideEffects`, one undo step, reproject). T1 oracle "[V] | a ↔ t | [C]", T2 compose, T4 edit round-trips (`RuleFormulaMetathesisTests`, 4 tests). // PARITY: middle-context (switch-cell edits blocked when MiddleIndex≠-1), move/reorder, and the result/swap-row display echo are deferred.
- [x] 2.4 DONE+green: the rule composes end-to-end through `<seq field="RightHandSides">` (proven by the record-level T2 `Compose_PhRegularRule_RecordPath_ResolvesTheRuleEditor`); `PhonologicalRuleEdit` registered in `LexicalEditSurfaceRegistry` and flipped Legacy→Avalonia in `RecordEditViewSwitchTests` (now resolves to Avalonia under UIMode=New — the rule tool is live, regular+metathesis, no Unsupported).
- [~] 2.5 READ-ONLY DONE+green: Input/Output roles; `RuleFormulaProjector.ProjectCompoundRule(IMoAffixProcess)` renders input columns ⇒ output mappings (copy-from-input→1-based column number, insert-phones→phoneme tokens, modify-from-input→"n[class]", insert-NC); `AffixRuleFormulaRegionEditorPlugin` claims `AffixRuleFormulaSlice` (registered). T1 oracle "[C][V] ⇒ 1a" + T2 compose `MoAffixProcess` ("Normal" layout) (`RuleFormulaCompoundTests`). // TODO: compound EDITING (input/output mutation + copy-index references) and the `compoundRuleAdvancedEdit` tool flip deferred (flipping now would make compound read-only under New UI).
- [x] 2.6 DONE+green (regular rule): `RuleFormulaEditWorkflowTests` drives insert/delete/reorder/atomic↔sequence → commit → re-project (reopen) round-trips, one undo step per gesture, undo restores. (Metathesis/compound get their own T4 with 2.3/2.5.)

## 3. Supporting bespoke editors
- [x] 3.1 DONE+green: `BasicIPASymbolEditor` editable entry; `BasicIpaSymbolEditSink` writes `BasicIPASymbol` + runs **derive-on-commit** (`BasicIpaSymbolDeriver` — ports the legacy `SetDescription/SetFeaturesBasedOnIPA`: fills empty Description per analysis WS + Features from `BasicIPAInfo.xml`, clears on empty; never overwrites user edits) in one fenced step. Tests: T4 commit/undo + derive/clear round-trip (`BasicIpaSymbolEditWorkflowTests`, real XML), T5 PNG, read-only test. `phonemeEdit` registered + flipped (IPA editor + derive + Features launcher + editable Codes vector + Name).
- [~] 3.2 EDITOR DONE+green: `PhEnvironmentEditor` (`Src/Common/FwAvalonia/Region/PhEnvironmentEditor.cs`) — editable validated string field + literal insert toolbar (#/[/]///_); on commit validates via the host's `PhonEnvRecognizer` and rejects+restores+errors on invalid. `PhEnvironmentRegionEditorPlugin` + `PhEnvironmentEditSink` (writes `StringRepresentation` in one fenced step) claim `PhEnvStrRepresentationSlice` (registered). Tests: T1 validate-reject + T4 commit/undo round-trip (`PhEnvironmentEditWorkflowTests`), T5 PNGs `-01-initial`/`-02-invalid` + commit/reject control tests (`PhEnvironmentEditorTests`). // PARITY: NC-insert chooser deferred; `EnvironmentEdit` tool not yet flipped (pending a compose verification of the full PhEnvironment detail). TODO localization: prototype error string.
- [x] 3.3 DONE+green: both NC subclasses compose fully editably — PhNCFeatures Features via the phonological-feature launcher; PhNCSegments Name/Desc/Abbrev + an editable Segments phoneme reference vector (via the new generic `ReferenceTargetCandidates` editable-vector path in `FullEntryRegionComposer.AddGenericReferenceVector`). `naturalClassedit` registered + flipped in `RecordEditViewSwitchTests`. `NaturalClassComposeTests` assert ReferenceVector + launcher compose.
- [~] 3.4 LEAF DONE+green: with the generic editable atomic-chooser + vector composer paths, `MoMorphAdhocProhib`/`MoAlloAdhocProhib` compose fully editably — Key (`FirstMorpheme`/`FirstAllomorph` → editable Chooser), Adjacency (EnumCombo), Others (`RestOfMorphs`/`RestOfAllos` → editable ReferenceVector), Active. `MoAdhocProhibGr` Name/Desc/Active editable. `AdhocCoprohibEdit` registered + flipped. T2 `AdhocCoProhibComposeTests` (Chooser+ReferenceVector for the leaf; Text+Boolean for the group). // PARITY: the nested `MoAdhocProhibGr.Members` recursive sub-prohibition lane composes read-only (editing deferred).
- [x] 3.5 DONE+green: T1/T4/T5 across the supporting editors — PhEnvStr (validate/commit/reject + PNGs), BasicIPASymbol (commit + derive/clear round-trip + PNG), natural-class compose (ReferenceVector + launcher), adhoc compose (Key Chooser + Others ReferenceVector + group), plus the rule-formula/metathesis/compound projector+edit suites. ~190 tests across the change. // PARITY: adhoc nested-group + MoAffixProcess editing deferred (read-only/compose verified).

## 4. Wiring + validation
- [x] 4.1 DONE+green: ALL SIX rule-family tools flipped Legacy→Avalonia (registered in `LexicalEditSurfaceRegistry` + `RecordEditViewSwitchTests` cases, all 12 green): **`PhonologicalRuleEdit`**, **`EnvironmentEdit`**, **`compoundRuleAdvancedEdit`** (// PARITY: `MoAffixProcess` read-only), **`naturalClassedit`**, **`phonemeEdit`** (IPA derive-on-commit), **`AdhocCoprohibEdit`** (leaf editable; // PARITY nested groups read-only). The proposal's full tool list is registered.
  - **TWO COMPOSER FIXES this session, both regression-free across the full managed suite** (only the 38 pre-existing environmental real-data/sentinel failures remain): (1) **multi-child part import** — `DictionaryPartResolver` returned only a part's first child, dropping the grammar `<if Disabled=true/false>` pair's active branch (Name/Description/category pickers vanished); added `IPartResolver.ResolvePartContents` + importer builds a node per child. (2) **generic editable reference vector** — `FullEntryRegionComposer.AddGenericReferenceVector` + `SafeReferenceTargetCandidates` (cap 500) make a non-`CmPossibilityList` vector editable via the field's `ReferenceTargetCandidates` (legacy chooser parity); (3) **generic editable atomic chooser** — `AddGenericAtomicChooser` (same, for ReferenceAtomic) — unblocked PhNCSegments.Segments, the ad-hoc Key, and the ad-hoc Others vector. census (`LexemeEditorBurnDownTests`) updated for the 5 new builtins.
  - **SCOPE/SAFETY (advisor-reviewed):** fixes (2)/(3) are in the SHARED `WalkOtherField` fallthrough (touches §20 infra used by live lexicon/notebook/POS). HARDENED: `SafeReferenceTargetCandidates` now gates out VIRTUAL/computed properties (`_mdc.get_IsVirtual` — the legacy `VectorReferenceView` editability gate), so back-refs/derived collections stay read-only (no blind Replace corruption). BLAST RADIUS measured: the lexicon/notebook/entry-vector/back-ref composition suites (192 tests) pass UNCHANGED — the live tools' fields are already special-cased before the fallthrough, so the generic paths only catch the grammar reference fields.
  - **SCOPE DECISION — RESOLVED: keep the generic global paths (sanctioned §20 composer enhancement).** Rationale (evidence-backed via legacy source map): WinForms FieldWorks editing is itself **entirely generic and metadata-driven** — `SliceFactory` builds ONE reused `AtomicReferenceSlice`/`ReferenceVectorSlice` for every object class, editability comes from the XML `editable` attr + field type + `IsVirtual` (`VectorReferenceView.cs:440`), candidates from the generic model method `ReferenceTargetCandidates(flid)`, and there is **ZERO per-class allow-list anywhere** in the reference stack. So the global path MIRRORS the legacy architecture; the "narrow to grammar classes" alternative would have introduced a per-class branch in shared plumbing that legacy deliberately avoids (an anti-pattern), added code, and forced every future tool to opt in — for no functional gain (both options are identical for the grammar tools: same chooser UI, same one-step undo via the fenced `RegionEditContext`, same candidates). The global path is legacy-faithful, hardened (`IsVirtual` gate), and blast-radius-verified.
- [x] 4.2 DONE+green: `EngineIsolationAuditTests` green — all new FwAvalonia controls (RuleFormulaRegionEditor, PhEnvironmentEditor, BasicIPASymbolEditor, RuleCellCommands) are LCModel/Views/Graphite-free.
- [x] 4.3 DONE+green: full managed `./build.ps1 -RunTests` — build 0 errors; the ONLY test failures are 38 PRE-EXISTING environmental (RenderComparison/RenderVerification real-data sentinel, MAIN-repo path, guard in a referenced lib — not this change) + 1 KNOWN-FLAKY clipboard/STA test (`TextDrag_…LegacyClipboardBridge…`, passes in isolation, unrelated to composer/reference changes). All three composer fixes (multi-child import, generic editable vector, generic editable atomic chooser) validated regression-free against the full suite. PNG baselines reviewed for every shipped editor (RuleFormulaEditor 01/02/03, PhEnvironmentEditor 01/02, BasicIPASymbolEditor 01). (Native skipped — already built; managed is the change surface.)
- [~] 4.4 `// PARITY` notes added throughout for every deferred affordance (feature-multiline NC + iteration in the projector; metathesis middle/move/swap-row; compound editing; IPA derive-on-commit; PhEnvStr NC-insert chooser; NC editable phoneme-vector; the shared atomic-reference/vector/AllA-multistring composer gap).
