# Tasks

## 1. Policy and Inventory (M0 → M1)

- [ ] 1.1 Adopt the Path A decision and record the Path B pivot-trigger threshold with the product owner (design.md Open Question 1); confirm the M2 definition ("Avalonia default for Lexical Edit + majority of `RecordEditView` consumers") as the sunset milestone.
- [ ] 1.2 Re-home the Graphite inventory tasks from `lexical-edit-avalonia-migration` section 5 (5.1–5.4) under this change: native code/assets, writing-system settings persistence, feature UI/storage classification, and the font-replacement/fallback policy. The Gecko/browser/PDF tasks (5.6/5.7) and the default-path native-engine audits (5.5/5.8, re-read as engine-isolation, not support-removal) stay with the lexical-edit change.
- [x] 1.3 Build the writing-system/font classification service (tiers G0–G3): inputs `IsGraphiteEnabled`, `DefaultFontFeatures`/per-WS feature overrides, and font-table sniffing (`Silf`/`Glat` vs `GSUB`/`GPOS`) of the resolved font file; deterministic, immutable-input, structured-diagnostic output (same diagnostics channel as the view-definition pipeline). Decide its home (design.md Open Question 3) so the LCModel-free table sniffer can be tested in `FwAvaloniaTests`. (Done 2026-06-09: `Src/Common/FwAvalonia/Graphite/` — `FontTableSniffer` (SFNT table-directory parse from file/bytes incl. TTC, plus GDI `GetFontData` for installed families with substitution rejection; unreadable input fails safe to `Unknown` → G0), `GraphiteWsClassifier` (deterministic G0–G3 with user-facing messages naming WS + font), `GraphiteFontTableEvidence`/`GraphiteWsClassification` with stable `graphite-gN` diagnostic codes. **Home decided: FwAvalonia (LCModel-free)**; the LCModel edge lives in `LexicalEditRegionBuilder.ClassifyWritingSystem` reading `CoreWritingSystemDefinition.IsGraphiteEnabled`/`DefaultFontFeatures`/`DefaultFontName`.)
- [x] 1.4 Validate the classifier against known fonts: Charis SIL/Doulos SIL/Scheherazade New (G1), a dual-engine font with Graphite feature settings fixture (G2), Awami Nastaliq (G3), and a Graphite-flag-without-Graphite-font fixture (G0). (Done 2026-06-09 against the repo's shipped fixtures, table-verified: `Src/views/Test/TestData/Fonts/CharisSIL-5.000/CharisSIL-R.ttf` is dual-engine (Silf+GSUB/GPOS → G1, G2 with features); `DistFiles/Graphite/pl/piglatin.ttf` and `sd/stakdiac.ttf` are Graphite-only (Silf, no GSUB/GPOS → G3, standing in for Awami Nastaliq which is not in-repo); `DistFiles/Graphite/ipa/SILDUb3.TTF` has neither table; flag-without-tables → G0 and flag-disabled-Graphite-only-font → G0 covered. `GraphiteClassificationTests`, 11 tests.)

## 2. Warning UX on Avalonia Surfaces (lands with the first user-text-rendering Avalonia surface)

- [ ] 2.1 Implement the graded warning: G1 log/setup-visible only; G2 once-per-project-session actionable warning; G3 prominent pre-render warning naming WS + font. All warnings carry the switch-to-Legacy-UI affordance (whole surface via the existing UI mode, never per-field) and a font-migration-guidance link; all texts localized per lexical-edit 6.11 rules with stable nonlocalized AutomationIds. (Mostly done 2026-06-09: `GraphiteWarningPolicy` grades presentation (G1 LogOnly, G2/G3 once per WS per project session via a per-`LcmCache` policy, G3 prominent + re-presented each session); `LexicalEditRegionView` renders graded banners (G3 bold/red-bordered) with stable `GraphiteWarningBanner.{wsId}[.Message|.SwitchToLegacy]` AutomationIds; the affordance flows `RecordEditView.SwitchProjectToLegacyUiMode` → `UIMode` property broadcast (the path RecordEditViewSwitchTests already prove). **Remaining:** the strings live in `GraphiteWarningStrings` pending the shared FwAvalonia `.resx` localization work (lexical-edit 6.11); the font-migration-guidance link target needs the 3.3 policy doc; per-field WS coverage beyond the first slice's default vern/anal WSs grows with the 6.x editors.)
- [x] 2.2 Record every G1–G3 determination as a structured diagnostic visible to support and included in Path 3 parity bundles. (Done 2026-06-09: `LexicalEditRegionBuilder.Build` appends every non-G0 determination to the region model's diagnostics as `ViewDiagnostic` (`graphite-g1` Info, `graphite-g2`/`graphite-g3` Warning, node path `ws/{wsId}`), the same channel parity bundles consume.)
- [x] 2.3 Add headless tests: tier → warning behavior mapping, per-session rate limiting, G3 never permanently suppressed while the condition holds, and the legacy-mode affordance wiring through `LexicalEditSurfaceSelectionService`. (Done 2026-06-09: `GraphiteWarningPolicyTests` (G0 none, G1 log-only forever, G2 once-per-WS-per-session, G3 prominent + reappears in a new session) and `GraphiteWarningBannerTests` (banner automation ids, message, affordance callback fires; no banner without warnings); the affordance reuses the UIMode broadcast → `LexicalEditSurfaceSelectionService` path covered by `RecordEditViewSwitchTests`. 17 new tests; FwAvaloniaTests 124/124, xWorks regression 9/9.)
- [x] 2.4 Replace the "Avalonia default blocked by Graphite" gate in region manifests with the "classification + warning coverage" gate; keep the forbidden-symbol audit (`GraphiteEngineClass`, `FwGrEngine`, `GraphiteSegment` never on the Avalonia path) unchanged. (Done 2026-06-09: `region-manifest.md` §4 rendering gate rewritten — G0–G3 classification + G2/G3 warning coverage is the gate, Graphite presence does not block, native-engine audit unchanged as a hard gate.)

## 3. Visibility and Measurement (M1 → M2 entry criteria)

- [ ] 3.1 Surface per-WS Graphite status (tier, font, what differs on the new editor) in the writing-system setup UI with migration guidance.
- [ ] 3.2 Run the LDML/project fixture scan quantifying G2/G3 prevalence across partner/test corpora; publish the result as the Path B pivot input and the M2 humane-enforcement evidence.
- [ ] 3.3 Publish the font-replacement policy: OpenType equivalents table for common Graphite feature uses; explicit "no replacement — stay on legacy until M3" entries (e.g. Awami Nastaliq) with owner contact.

## 4. Sunset (M2) and Removal (M3)

- [ ] 4.1 Ship migration tooling before M2 enforcement: Graphite feature-string → OpenType feature mapping where equivalents exist, font-replacement assistant; rewrites `IsGraphiteEnabled`/`DefaultFontFeatures` only on explicit user action, with undo; tested on the 3.2 fixture corpus.
- [ ] 4.2 M2 deprecation sequence: in-app deprecation notice with timeline for existing Graphite projects; block new Graphite enablement (with the exemption decision from design.md Open Question 2); at least one release of advance notice before enforcement.
- [ ] 4.3 Direct outreach to G3-classified strategic-language projects identified by the 3.2 scan before enforcement.
- [ ] 4.4 M3 removal (with WinForms/native Views deletion): `Src/views/lib/GraphiteEngine.*`, `GraphiteSegment`, render-engine Graphite selection, Gecko Graphite preference, Graphite-specific tests/sample assets/build artifacts; settings storage handled per the lexical-edit data-migration rules (values preserved, semantics retired).

## 5. Validation

- [ ] 5.1 Verify no Avalonia-path reference to native Graphite symbols at every milestone (existing region-manifest symbol audit — unchanged by this policy).
- [ ] 5.2 Verify warning coverage: every G2/G3 writing system in the fixture corpus produces its warning on an Avalonia surface, exactly once per session, with working legacy-mode affordance.
- [ ] 5.3 Verify legacy surfaces' Graphite rendering is bit-identical to pre-migration baselines (existing render-verification harness) at M1 and M2.
- [ ] 5.4 Verify M2 enforcement is gated on shipped tooling + completed scan + advance notice, not on the calendar.
