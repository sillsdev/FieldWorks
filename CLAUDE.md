@AGENTS.md

## Claude Code

- Keep repo-wide standing guidance in `AGENTS.md` and import it here.
- Put Claude-only workflows and task procedures under `.claude/skills/`.
- Keep `.github/` only for GitHub-required compatibility files such as
  workflows, PR templates, `copilot-instructions.md`, and
  `.github/instructions/*.instructions.md`.