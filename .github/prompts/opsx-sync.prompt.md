---
description: Sync delta specs from a change to main specs
agent: "agent"
argument-hint: "Optional change name"
---

Use the `openspec-sync-specs` skill from `.claude/skills/openspec-sync-specs/SKILL.md`.

Treat any text supplied with this prompt as the optional change name. If omitted, the skill will list changes that have delta specs and prompt for selection.
