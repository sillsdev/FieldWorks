# Lex Reference Unidirectional Launcher (`LexReferenceUnidirectionalLauncher`)

| | |
|---|---|
| **Legacy class** | `SIL.FieldWorks.XWorks.LexEd.LexReferenceUnidirectionalLauncher` (`Src/LexText/Lexicon/LexReferenceUnidirectionalLauncher.cs`) |
| **Area** | Lexicon |
| **Type** | launcher |
| **Primitive** | owned-control |
| **State** | coexist |
| **Phase** | 1 |
| **Canonical reference** | ChooserDialog |
| **JIRA** | LT-XXXXX |

## What it is
Owned-control slice launcher for a UNIDIRECTIONAL lexical-relation reference.

## Notes / gotchas
- Subclass of VectorReferenceLauncher. State=coexist (UIMode-gated). Lexical-relations slice family.

> Stub. Deepen using `Docs/migration/_TEMPLATE.md` (capture legacy PNGs via the `fieldworks-winapp` skill) when this ticket is picked up.

