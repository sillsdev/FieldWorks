# Missing Old FieldWorks (`MissingOldFieldWorksDlg`)

| | |
|---|---|
| **Legacy class** | `SIL.FieldWorks.FwCoreDlgs.MissingOldFieldWorksDlg` (`Src/FwCoreDlgs/MissingOldFieldWorksDlg.cs`) |
| **Area** | App-wide (restore / migration) |
| **Type** | dialog |
| **Primitive** | plain-form |
| **State** | legacy |
| **Phase** | 1 |
| **Canonical reference** | plain-form→nearest |
| **JIRA** | LT-XXXXX |

## What it is
Pops up when the user tries to restore/migrate an old project but the old version of FieldWorks (or its special SQL Server instance) is not installed.

## Notes / gotchas
- Informational/blocking dialog; modal.

> Stub. Deepen using `Docs/migration/_TEMPLATE.md` (capture legacy PNGs via the `fieldworks-winapp` skill) when this ticket is picked up.
