# Merge Writing System (`MergeWritingSystemDlg`)

| | |
|---|---|
| **Legacy class** | `SIL.FieldWorks.FwCoreDlgs.MergeWritingSystemDlg` (`Src/FwCoreDlgs/MergeWritingSystemDlg.cs`) |
| **Area** | App-wide (writing-system management) |
| **Type** | dialog |
| **Primitive** | plain-form |
| **State** | legacy |
| **Phase** | 1 |
| **Canonical reference** | plain-form→nearest |
| **JIRA** | LT-XXXXX |

## What it is
The merge writing systems dialog — merges one writing system's data into another.

## Notes / gotchas
- Modal; data-mutating operation (confirm semantics on pickup).

> Stub. Deepen using `Docs/migration/_TEMPLATE.md` (capture legacy PNGs via the `fieldworks-winapp` skill) when this ticket is picked up.
