# Word Analyses (`Analyses`)

| | |
|---|---|
| **Tool id** | `Analyses` |
| **Area** | Texts & Words |
| **Type** | tool-screen |
| **Surface** | interlinear |
| **Primitive** | interlinear |
| **State** | split-out (own PR) |
| **Phase** | 1 |
| **Canonical reference** | n/a |
| **JIRA** | LT-XXXXX |

## What it looks like
![Word Analyses (`Analyses`) (Sena 3, Legacy)](./images/analyses-01.png)

## What it is
Word Analyses morph-bundle editor (per-wordform analyses).

## Notes / gotchas
- Control is RecordEditView (DataTree detail) with WordsEditToolMenuHandler, but the task frames this as the interlinear analyses editor.
- Surface interlinear; State split-out (own PR).

> Stub. Deepen using `Docs/migration/_TEMPLATE.md` (capture legacy PNGs via the `fieldworks-winapp` skill) when this ticket is picked up.
