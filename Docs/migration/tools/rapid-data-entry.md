# Collect Words (`rapidDataEntry`)

| | |
|---|---|
| **Tool id** | `rapidDataEntry` |
| **Area** | Lexicon |
| **Type** | tool-screen |
| **Surface** | browse (RecordBrowseView/table) |
| **Primitive** | TABLE |
| **State** | legacy (default) |
| **Phase** | 1 |
| **Canonical reference** | browse/table -> Lexicon Browse pane (LexicalBrowseView) |
| **JIRA** | LT-XXXXX |

## What it is
Rapid Data Entry: collect words by semantic domain (RDE).

## Notes / gotchas
- Composite: semantic-domain tree/record list + RecordBrowseView + RecordEditView edit pane (SemanticCategoryAndItems MultiPane).
- Defining surface is the RDE browse/collect grid; secondary RecordEditView detail pane.
- Driven by a PossibilityRecordList over Semantic Domains.

> Stub. Deepen using `Docs/migration/_TEMPLATE.md` (capture legacy PNGs via the `fieldworks-winapp` skill) when this ticket is picked up.
