c This is used to process header files generated by the code generator to
c produce class.cc which converts @ class names to their class numbers.

group(main)
endfile > endfile
'#define kclid' > store(Name) use(Copy)
'' > omit

group(Copy)
' ' > store(Num)
nl > endstore "'@" out(Name) "' > '" out(Num) "'" nl
	use(main)
