<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <UseWindowsForms>true</UseWindowsForms>
    <RootNamespace>ParatextImport</RootNamespace>
    <Description>ParatextImport</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Encoding-Converters-Core" Version="0.5.1" PrivateAssets="native" />
    <PackageReference Include="GitVersion.MsBuild" Version="5.10.3" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FwCoreDlgs\FwCoreDlgs.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Properties\Resources.Designer.cs">
      <DesignTime>True</DesignTime>
      <AutoGen>True</AutoGen>
      <DependentUpon>Resources.resx</DependentUpon>
    </Compile>
    <EmbeddedResource Update="Properties\Resources.resx">
      <Generator>ResXFileCodeGenerator</Generator>
      <LastGenOutput>Resources.Designer.cs</LastGenOutput>
    </EmbeddedResource>
  </ItemGroup>

  <Target Name="CopyEncConverterPlugins" AfterTargets="Build">
    <Copy SourceFiles="@(EncodingConvertersPlugins)" DestinationFolder="$(OutputPath)\EC\Plugins" SkipUnchangedFiles="true" />
  </Target>
</Project>