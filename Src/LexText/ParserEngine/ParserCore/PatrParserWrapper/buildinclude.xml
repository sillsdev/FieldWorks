<?xml version="1.0"?>
<include xmlns="http://fieldworks.sil.org/nant/fwnant.xsd">
  <pre-build>
	<if test="${platform::is-unix()}">
	<copy todir="${dir.buildOutput}">
	  <fileset>
		<include name="${dir.srcProj}/PatrParserWrapper.dll.config"/>
		<include name="${fwroot}/Lib/linux/libpatr64.so"/>
		<include name="${fwroot}/Lib/linux/libpatr32.so"/>
	  </fileset>
	</copy>
	</if>
  </pre-build>
</include>
